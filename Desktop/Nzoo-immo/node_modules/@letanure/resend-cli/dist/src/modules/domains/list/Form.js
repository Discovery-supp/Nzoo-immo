import { jsx as _jsx } from "react/jsx-runtime";
import { ListDisplay } from '../../../components/ui/ListDisplay.js';
import { listDomains } from './action.js';
import { displayFields } from './fields.js';
export const Form = ({ onExit }) => {
    const formatData = (data) => {
        return data.data.map((domain) => ({
            id: domain.id,
            name: domain.name,
            status: domain.status,
            region: domain.region || '',
            created_at: new Date(domain.created_at).toLocaleString(),
        }));
    };
    return (_jsx(ListDisplay, { title: "Domains - List", onExit: onExit, loadFunction: listDomains, displayFields: displayFields, formatData: formatData, noDataMessage: "No domains found." }));
};
//# sourceMappingURL=Form.js.map