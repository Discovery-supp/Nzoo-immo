import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { useState } from 'react';
import { Form as CreateForm } from './create/Form.js';
import { Form as DeleteForm } from './delete/Form.js';
import { Form as ListForm } from './list/Form.js';
import { Menu, MenuState } from './Menu.js';
import { Form as RetrieveForm } from './retrieve/Form.js';
import { Form as UpdateForm } from './update/Form.js';
import { Form as VerifyForm } from './verify/Form.js';
export const App = ({ onExit }) => {
    const [screenState, setScreenState] = useState('menu');
    const [lastSelectedDomainMenuItem, setLastSelectedDomainMenuItem] = useState();
    const handleMenuSelect = (menuId) => {
        setScreenState(menuId);
    };
    return (_jsxs(_Fragment, { children: [screenState === 'menu' && (_jsx(Menu, { onSelect: handleMenuSelect, onExit: () => onExit(), initialSelectedKey: lastSelectedDomainMenuItem })), screenState === 'create' && (_jsx(CreateForm, { onExit: () => {
                    setLastSelectedDomainMenuItem(MenuState.create);
                    setScreenState('menu');
                } })), screenState === 'retrieve' && (_jsx(RetrieveForm, { onExit: () => {
                    setLastSelectedDomainMenuItem(MenuState.retrieve);
                    setScreenState('menu');
                } })), screenState === 'verify' && (_jsx(VerifyForm, { onExit: () => {
                    setLastSelectedDomainMenuItem(MenuState.verify);
                    setScreenState('menu');
                } })), screenState === 'update' && (_jsx(UpdateForm, { onExit: () => {
                    setLastSelectedDomainMenuItem(MenuState.update);
                    setScreenState('menu');
                } })), screenState === 'list' && (_jsx(ListForm, { onExit: () => {
                    setLastSelectedDomainMenuItem(MenuState.list);
                    setScreenState('menu');
                } })), screenState === 'delete' && (_jsx(DeleteForm, { onExit: () => {
                    setLastSelectedDomainMenuItem(MenuState.delete);
                    setScreenState('menu');
                } }))] }));
};
//# sourceMappingURL=App.js.map