import { jsx as _jsx } from "react/jsx-runtime";
import { ListDisplay } from '../../../components/ui/ListDisplay.js';
import { listAudiences } from './action.js';
import { displayFields } from './fields.js';
export const Form = ({ onExit }) => {
    const formatData = (data) => {
        return data.data.map((audience) => ({
            id: audience.id,
            name: audience.name,
            created_at: new Date(audience.created_at).toLocaleString(),
        }));
    };
    return (_jsx(ListDisplay, { title: "Audiences - List", onExit: onExit, loadFunction: listAudiences, displayFields: displayFields, formatData: formatData, loadData: {}, noDataMessage: "No audiences found." }));
};
//# sourceMappingURL=Form.js.map