export interface MenuItem<T extends string> {
    id: T;
    label: string;
    description: string;
    disabled?: boolean;
}
interface MainMenuProps<T extends string> {
    menuItems: Array<MenuItem<T>>;
    onSelect: (menuId: T) => void;
    onExit: (lastSelectedKey?: T) => void;
    title?: string;
    initialSelectedKey?: T;
    isRootMenu?: boolean;
}
export declare const Menu: <T extends string>({ menuItems, onSelect, onExit, initialSelectedKey, isRootMenu, }: MainMenuProps<T>) => import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=Menu.d.ts.map