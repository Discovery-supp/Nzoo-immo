import type { ApiResult, CliField } from '../../types/index.js';
interface ListDisplayProps<T, D = Record<string, unknown>> {
    title: string;
    onExit: () => void;
    loadFunction: (data: D, apiKey: string) => Promise<ApiResult<T>>;
    displayFields: Array<CliField>;
    formatData: (data: T) => Array<Record<string, unknown>>;
    loadData?: D;
    noDataMessage?: string;
}
export declare function ListDisplay<T, D = Record<string, unknown>>({ title, onExit, loadFunction, displayFields, formatData, loadData, noDataMessage, }: ListDisplayProps<T, D>): import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=ListDisplay.d.ts.map