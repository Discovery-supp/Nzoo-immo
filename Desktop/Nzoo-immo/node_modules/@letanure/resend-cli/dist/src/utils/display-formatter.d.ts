import type { FormField } from '../types/index.js';
export interface FormattedField {
    label: string;
    value: string;
}
/**
 * Generic function to format data using field configuration
 * Returns array of formatted fields that can be displayed by CLI or TUI
 */
export declare function formatDataWithFields(data: Record<string, unknown>, fields: Array<FormField>, fieldsToShow?: Array<string>): Array<FormattedField>;
/**
 * Format fields for CLI display with dynamic padding based on longest label
 */
export declare function formatForCLI(formattedFields: Array<FormattedField>, title?: string): string;
//# sourceMappingURL=display-formatter.d.ts.map